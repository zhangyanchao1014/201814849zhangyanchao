net noise owner subject fact 30 bit serial number possibly fixed s1 s2 way view thi method generating unit key else call key generated serial number generate unit key serial number n value n first fixed block produce 64bit block n1 s1 s2 used key n1 producing 64bit block s1 s2 s1 similarly n two block produce two additional 64bit block r3 s1 s2 s1 r3 s1 s2 s1 r3 together giving 192 bit first 80 bit assigned u1 second 80 bit u2 rest discarded unit key u xor u1 u2 u1 u2 key part separately escrowed two escrow agency happen need escrow house completely eliminated say need acces escrow house decrypt datum house still serve real purpose far generating illusion protection cop wont let secret still go motion going escrow house fact federal government build chip generate unit key encrypted serial number said
