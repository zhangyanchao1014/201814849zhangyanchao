bruce subject title xterm article mark write hint sun user use instead csh builtin echo otherwise youll literal character string instead using convenient sequence bad idea using slower builtin echo thi execute hostname per shell window read one extra file title require extra program oh ye execute program per system store file next time hostname hostname hostname endif figure generate escape bell echo command without line terminator may done thi variable e set executed thi script thi system e echo already set echo else f source else echo n wc l 0 echo built echo berkeley style echo echo set echo n echo set e echo echo set b echo echo set n source else echo built echo echo set echo echo set e echo set b echo set n c source endif using xterm duplicate avoid problem term sun sun alia header alia else term xterm alia header alia endif bruce
