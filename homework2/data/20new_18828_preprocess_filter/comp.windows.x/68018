scott frost subject draw mode color draw object specified color object varying color erase without redraw everything else happening thi draw using drawn specified color erase using background reversed erase whatever need redraw item crossed draw using drawn random color nothing requested properly restore color underlying object erase mode solution scott k frost uucp bitnet personal rd upper oh
