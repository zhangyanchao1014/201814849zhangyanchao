mark w subject noise small signal never worked really small signal question low level noise noticed using ha relatively large common mode noise using thi null another difference several hundred time ha 13 common mode noise thi tend make measurement hard repeat noise quite sync line frequency spectrum main component get rid noise look spectrum using hp audio range noise go away minute switch isolation input guessing ha diff amp input since read accurately dc also use differential amplifier ia solve problem important use one shield driver amp connected shield sort feedback loop remove trouble caused different cable anyone suggest good ic ia use signal content would appreciate
